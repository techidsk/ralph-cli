# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Ralph CLI 全局默认配置
# 优先级: 环境变量 > ~/.ralph/config.yaml > 此文件
# 安装时会复制为 ~/.ralph/config.yaml（首次安装）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── 任务源 ──

# 任务源类型: repo | gist | local
RALPH_SOURCE: repo

# Git 仓库地址（repo 源）
# RALPH_INBOX_REPO: git@github.com:your-user/your-inbox.git

# GitHub Gist ID（gist 源）
# RALPH_GIST_ID:

# ── 项目 ──

# 项目根目录（默认从 git rev-parse --show-toplevel 推导，通常不需要手动设置）
# RALPH_CWD: /path/to/your/project

# 无任务时轮询间隔（秒）
RALPH_POLL_INTERVAL: 600

# ── 模型 ──

# 任务执行默认模型
# RALPH_DEFAULT_MODEL: claude-sonnet-4-6

# Reflect 阶段模型
# RALPH_REFLECT_MODEL: claude-sonnet-4-6

# Verify 阶段 review 模型
# RALPH_REVIEW_MODEL: claude-haiku-4-5-20251001

# ── 执行限制 ──

# 简单任务最大轮次
# RALPH_MAX_TURNS_SIMPLE: 15

# 复杂任务最大轮次
# RALPH_MAX_TURNS_COMPLEX: 30

# ── 通知 ──

# Webhook URL（留空则不发送通知）
# 支持飞书自定义机器人、钉钉、Slack 等任何接收 JSON POST 的地址
# RALPH_WEBHOOK_URL: https://hooks.example.com/your-webhook
